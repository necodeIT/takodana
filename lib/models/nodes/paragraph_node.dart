part of takodana_engine;

/// ParagraphNode is a [SyntaxNode] that represents a paragraph.
///
/// It it does not hold any text.
///
/// Used to group other nodes.
class ParagraphNode extends MetaNode {
  /// ParagraphNode is a [SyntaxNode] that represents a paragraph.
  ///
  /// It it does not hold any text.
  ///
  /// Used to group other nodes.
  ParagraphNode() : super();
}
